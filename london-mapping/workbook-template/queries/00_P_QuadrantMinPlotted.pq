// Query name: P_QuadrantMinPlotted
//
// How many qualifying peers a borough or trust needs before its median means
// anything. Below this, every school in it is left blank rather than
// classified against three other schools.
//
// 5 matches MIN_PLOTTED in js/ui/quadrant.js.
let
    Source = 5
in
    Source
