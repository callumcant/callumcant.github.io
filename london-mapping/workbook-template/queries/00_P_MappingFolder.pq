// Query name: P_MappingFolder
//
// Path from the library root down to the mapping data, one list item per
// folder. A subfolder of "London data (do not delete)" rather than its root,
// because the root is already in use for other things.
let
    Source = {"London data (do not delete)", "Mapping"}
in
    Source
